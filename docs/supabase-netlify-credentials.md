# Credentials needed to complete deployment

Send these values when you are ready for me to connect the live deployment:

## Supabase

- Project URL: `https://xxxxx.supabase.co`
- Anon/public key
- Service role key

## Netlify

The local Netlify CLI is already logged in as `deepanshu@indibuying.com`, but this folder is not linked to a Netlify site yet.

If you want me to create/link the Netlify site from here, confirm the Netlify site name you want. Suggested:

```text
gtmflows
```

## Slack

- Slack webhook URL for contact form delivery.

## DNS

After the Netlify preview is tested, Netlify will provide DNS records for:

```text
gtmflows.co
www.gtmflows.co
```

Those should be changed only after the Netlify version passes QA.
